#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

#define fi first
#define se second

using namespace std;

typedef long long ll;
typedef pair<int, int> pp;

vector<ll> v(65);

int main() {
  cin.tie(0), cout.tie(0), ios::sync_with_stdio(0);
  for (int i = 1; i < 65; i++) {
  }

  return 0;
}
