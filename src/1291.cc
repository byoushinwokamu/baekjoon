#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

#define fi first
#define se second

using namespace std;

typedef long long ll;
typedef pair<int, int> pp;

int main() {
  cin.tie(0), cout.tie(0), ios::sync_with_stdio(0);
  // absolute : 완벽한 숫자 4와 태초의 숫자들 2,3의 합으로 표현되는 숫자들 ->
  // 6,7,8,9,10, ...
  // 에 해당하는 수가 이면수
  // 임현수: 2,4,또는 소인수분해했을 때 소인수 종류가 짝수개
  // 둘다 아니면 성경수
  // 이면수 1 임현수 2 둘다아니면 3 둘다면 4
  int n;
  cin >> n;

  return 0;
}
