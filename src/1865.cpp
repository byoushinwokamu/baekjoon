#include <iostream>
#include <queue>

using namespace std;
int adj[500][500];

int main() {
  cin.tie(NULL);
  cout.tie(NULL);
  ios::sync_with_stdio(false);

  int tc;
  cin >> tc;
  for (int tt = 0; tt < tc; tt++) {
    int n, m, w;
    cin >> n >> m >> w;
  }

  return 0;
}